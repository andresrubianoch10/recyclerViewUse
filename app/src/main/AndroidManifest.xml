<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recyclerviewuse">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecyclerViewUse">
        <activity
            android:name=".staggeredGrid.StaggeredGridActivity"
            android:exported="false" />
        <activity
            android:name=".gridLayout.GridActivity"
            android:exported="false" />
        <activity
            android:name=".linearHorizontal.LinearHorizontalActivity"
            android:exported="false" />
        <activity
            android:name=".linearVertical.LinearVerticalActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>